/** Caminho relativo ao `prisma/schema.prisma`, que é como o Prisma resolve URL de SQLite. */
export const TEST_DATABASE_URL = "file:../data/test.db";

/** O mesmo arquivo, relativo à raiz do projeto, para apagar antes de migrar. */
export const TEST_DATABASE_FILE = "data/test.db";
