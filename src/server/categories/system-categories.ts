/**
 * Categorias que o app mantém e referencia pelo nome. Ficam num módulo puro porque tanto o
 * serviço quanto o seed precisam delas, e o seed não pode carregar código de request.
 */
export const YIELD_CATEGORY_NAME = "Rendimentos";
